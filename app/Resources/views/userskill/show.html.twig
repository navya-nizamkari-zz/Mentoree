{% extends 'base.html.twig' %}

{% block body %}
    <h1>UserSkill</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ userSkill.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('userskill_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('userskill_edit', { 'id': userSkill.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
